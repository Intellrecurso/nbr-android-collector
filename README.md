# Carrier Smart Remote

Android phone remote controller for a Carrier split air conditioner. Supports Android IR hardware when configured with the correct raw code profile and local Wi-Fi control through an ESP32 IR bridge.

Build status: GitHub Actions compiles an installable debug APK and publishes it as the CarrierSmartRemote workflow artifact.
